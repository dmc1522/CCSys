<UserControl x:Class="LasMargaritas.UI.UserControls.ProducerList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LasMargaritas.UI.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="1920">
    <Grid>
        <ListView Margin="10" Name="ProducerListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="40" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Nombre" Width="550" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Apellido" Width="550" DisplayMemberBinding="{Binding LastName}" />
                    <GridViewColumn Header="Gaffete" Width="150">
                        <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="PrintGaffete" Content="Imprimir gafette" Click="PrintGaffete_Click"  CommandParameter="{Binding}"/>
                        </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
